var calcCircleArea = function(r){
    var area = (Math.PI*r*r);
    return("r = " + r + "; area = "+ area);
};
var result = calcCircleArea(7);
var resultTwo = calcCircleArea(1.5);
var resultThree = calcCircleArea(20);

